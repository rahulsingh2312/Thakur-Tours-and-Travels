[build]
  command = "npm run build"
  publish = "build/"

[build.environment]
  NODE_VERSION = "14"

[build.headers]
  # Set Security Headers here if needed

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
